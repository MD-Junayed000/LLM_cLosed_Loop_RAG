\chapter{Experimental Setup and Results}
\label{ch:results}

This is where the rubber meets the road. We evaluate the full CEV/IAV closed-loop framework on three frozen decoder backbones—\textbf{Mistral-7B-Instruct-v0.2}, \textbf{LLaMA-3-8B-Instruct}, and \textbf{Qwen3-8B}—and ask four questions. Can the probes actually detect hallucination reliably? Does fusing two signals beat using one alone? Does the closed-loop controller genuinely reduce hallucination risk compared to vanilla RAG? And do the learned signals generalize to data the probes have never seen?

%% -------------------------------------------------
\section{Experimental Setup}
\label{sec:experimental_setup}

Every experiment runs on Google Colab with an NVIDIA A100 GPU (40\,GB) under FP16 inference. The backbone weights are frozen throughout—we never fine-tune the language model itself. Only the lightweight probes are trained, and each backbone gets its own independently calibrated probes, thresholds, and fusion weights, all tuned on a stratified RAGTruth validation split.

%% -------------------------------------------------
\section{Dataset Overview}
\label{sec:dataset_overview}

We rely on four datasets, each serving a distinct role in the pipeline. Table~\ref{tab:dataset_roles} lays out who does what.

\begin{table}[!htb]
\centering
\caption{Dataset roles in the experimental pipeline.}
\label{tab:dataset_roles}
\small
\begin{tabular}{@{}l r l c c c p{3.5cm}@{}}
\toprule
\textbf{Dataset} & \textbf{Samples} & \textbf{Role} & \textbf{Train?} & \textbf{Retrieval?} & \textbf{Eval?} & \textbf{Purpose} \\
\midrule
RAGTruth-18K & 15{,}090 + 3{,}058 & Probe supervision & Yes & No & Val split & Train/validate probes \\
SQuAD v1.1 (FAISS) & 25{,}000 & Retrieval KB & No & Yes & No & Build FAISS index \\
SQuAD v1.1 (F1) & 10{,}570 & Utility check & No & No & Optional & Token-level F1 \\
HaluEval-QA & 10{,}000 & OOD benchmark & No & No & Yes & Cross-domain transfer \\
HalluRAG & 1{,}098 & Supplementary & Mistral & No & No & Sentence-level examples \\
\bottomrule
\end{tabular}
\end{table}

\subsection{RAGTruth-18K: Primary Probe Supervision}
\label{sec:ragtruth}

RAGTruth~\cite{wu2024ragtruth} gives us response-level hallucination labels for RAG-generated text. We convert these into a simple binary scheme: $y = 0$ means the response is grounded (supported by the retrieved context), and $y = 1$ means it is hallucinated (unsupported or contradictory). This dataset provides the training signal and validation benchmark for our probes.

\subsection{SQuAD v1.1: Retrieval Corpus and Operational QA Source}
\label{sec:squad}

SQuAD v1.1~\cite{rajpurkar2016squad} pulls double duty. Its 25{,}000 training-set contexts become the FAISS retrieval index—the knowledge base from which our system pulls evidence at inference time. Meanwhile, its validation QA pairs supply the operational queries we use to test the full closed-loop pipeline.

\subsection{HaluEval-QA: Out-of-Distribution Evaluation}
\label{sec:halueval_dataset}

HaluEval~\cite{li2023halueval} pairs correct answers with hallucinated alternatives. We score both with our trained probes and measure AUROC—can the probe tell them apart even though it was never trained on this data? This is our primary generalization test.

\subsection{HalluRAG: Mistral-Specific Supplementary Data}
\label{sec:hallurag}

HalluRAG~\cite{frohling2024hallurag} offers sentence-level hallucination labels for Mistral-generated RAG outputs. We use it as optional supplementary training data for the Mistral backbone specifically.

%% -------------------------------------------------
\section{Dataset Merging and Split Protocol}
\label{sec:split_protocol}

Table~\ref{tab:dataset_summary} summarizes the full data landscape at a glance.

\begin{table}[!htb]
\centering
\caption{Dataset summary.}
\label{tab:dataset_summary}
\small
\begin{tabular}{@{}lrlll@{}}
\toprule
\textbf{Dataset} & \textbf{Samples} & \textbf{Role} & \textbf{Models} & \textbf{Source} \\
\midrule
RAGTruth-18K & 15{,}090 + 3{,}058 & Probe supervision & All & \cite{wu2024ragtruth} \\
SQuAD v1.1 (FAISS) & 25{,}000 & Retrieval KB & All & \cite{rajpurkar2016squad} \\
SQuAD v1.1 (F1) & 10{,}570 & Utility eval & All & \cite{rajpurkar2016squad} \\
HaluEval-QA & 10{,}000 & OOD AUROC & All & \cite{li2023halueval} \\
HalluRAG & 1{,}098 & Supplementary & Mistral only & \cite{frohling2024hallurag} \\
\bottomrule
\end{tabular}
\end{table}

%% -------------------------------------------------
\section{Probe Training Details}
\label{sec:probe_training_details}

\subsection{Loss Function and Class-Weight Calculation}
\label{sec:loss_function}

Each probe is a binary classifier ($y \in \{0, 1\}$) trained with class-weighted cross-entropy and label smoothing:

\begin{equation}
    \mathcal{L} = -\sum_{c \in \{0,1\}} w_c \cdot \tilde{y}_c \cdot \log\bigl(\text{softmax}(\mathbf{z})_c\bigr)
    \label{eq:loss}
\end{equation}

\noindent The class weights correct for the mild label imbalance in RAGTruth. With roughly \textbf{8{,}369 grounded} and \textbf{6{,}721 hallucinated} examples ($N = 15{,}090$ total), inverse-frequency weighting gives:

\begin{align}
    w_{\text{grounded}} &= \frac{15{,}090}{2 \times 8{,}369} = 0.902 \\[4pt]
    w_{\text{hallucinated}} &= \frac{15{,}090}{2 \times 6{,}721} = 1.122
\end{align}

\noindent So the hallucinated class gets a slightly higher weight since it is less frequent—nothing dramatic, but enough to prevent the probe from being lazy and always predicting ``grounded.''

\subsection{Learning-Rate Explanation}
\label{sec:lr_explanation}

Not all backbones are created equal when it comes to probe training. Mistral's activations turned out to be more sensitive to prompt formatting, so we gave it a gentler learning rate. LLaMA and Qwen converged fine with a faster step.

\begin{table}[!htb]
\centering
\caption{Learning-rate settings and rationale.}
\label{tab:lr_settings}
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Backbone} & \textbf{Learning Rate} & \textbf{Why} \\
\midrule
Mistral-7B-Instruct-v0.2 & $3 \times 10^{-4}$ & More conservative; activations are format-sensitive \\
LLaMA-3-8B-Instruct      & $1 \times 10^{-3}$ & Converges quickly; stable activation distributions \\
Qwen3-8B                  & $1 \times 10^{-3}$ & Strong validation signal; fast convergence \\
\bottomrule
\end{tabular}
\end{table}

%% -------------------------------------------------
\section{Threshold Selection and Deployment Calibration}
\label{sec:threshold_selection}

Each backbone needs its own intervention and abstention thresholds because their probe-score distributions differ. We select these on the validation split using F1 optimization for the intervention threshold and a safety margin above it for abstention.

\begin{table}[!htb]
\centering
\caption{Deployment thresholds per backbone.}
\label{tab:deploy_thresholds}
\begin{tabular}{@{}lccp{5cm}@{}}
\toprule
\textbf{Backbone} & $\theta_{\text{hallu}}$ & $\theta_{\text{abs}}$ & \textbf{Interpretation} \\
\midrule
Mistral-7B & 0.62 & 0.75 & Conservative; avoids excessive false positives \\
Qwen3-8B   & 0.55 & 0.72 & Lower threshold thanks to stronger score separation \\
LLaMA-3-8B & 0.60 & 0.78 & Higher abstention bar to reduce over-refusal \\
\bottomrule
\end{tabular}
\end{table}

%% -------------------------------------------------
\section{Primary RAGTruth Validation Results}
\label{sec:primary_results}

Table~\ref{tab:ragtruth_results} presents the headline numbers. All three backbones achieve fused AUROC values above 0.85, with Qwen3-8B leading the pack at 0.8808.

\begin{table}[!htb]
\centering
\caption{RAGTruth validation results (primary in-domain evaluation).}
\label{tab:ragtruth_results}
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Backbone} & \textbf{CEV AUROC} & \textbf{IAV AUROC} & \textbf{Fused AUROC} & \textbf{Mean Acc.} \\
\midrule
Mistral-7B-Instruct-v0.2 & 0.8474 & 0.8474 & 0.8596 & 0.7806 \\
Qwen3-8B                  & \textbf{0.8763} & \textbf{0.8709} & \textbf{0.8808} & \textbf{0.7995} \\
LLaMA-3-8B-Instruct      & 0.8579 & 0.8622 & 0.8689 & 0.7822 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!htb]
    \centering
    \includegraphics[width=0.90\textwidth]{Images/our_models_multi_metric.png}
    \caption[Cross-model probe performance.]{Cross-model probe performance across multiple metrics. Qwen3-8B leads on in-domain validation; LLaMA-3-8B-Instruct leads on out-of-domain HaluEval transfer.}
    \label{fig:multi_metric}
\end{figure}

%% -------------------------------------------------
\section{Probe Score Distributions}
\label{sec:score_distributions}

Figure~\ref{fig:score_histograms} shows what the probe scores look like in practice. Grounded and hallucinated examples produce overlapping but distinguishable distributions—which is exactly what you would expect for a phenomenon as nuanced as hallucination. Perfect separation is unrealistic; what matters is that the overlap is small enough for useful thresholding.

\begin{figure}[!htb]
    \centering
    \includegraphics[width=0.90\textwidth]{Images/probe_score_histograms_grid.png}
    \caption[Probe score distributions.]{Probe score distributions for grounded vs.\ hallucinated examples. Partial overlap is expected—hallucination is a graded phenomenon, not a binary one.}
    \label{fig:score_histograms}
\end{figure}

%% -------------------------------------------------
\section{Baseline Comparison on RAGTruth}
\label{sec:baseline_comparison}

How does our approach stack up against existing detectors? Table~\ref{tab:baselines} lines up ten published or reproduced baselines against our fused CEV/IAV probe. The comparison is telling: our method outperforms every baseline on both Mistral and LLaMA, and it is the only one with closed-loop control capability built in.

\begin{table}[!htb]
\centering
\caption{Comparison with published hallucination-detection baselines on RAGTruth.}
\label{tab:baselines}
\small
\begin{tabular}{@{}l l c cc@{}}
\toprule
\textbf{Method} & \textbf{Signal Type} & \textbf{CL?} & \textbf{Mistral} & \textbf{LLaMA} \\
\midrule
SelfCheckGPT~\cite{manakul2023selfcheckgpt}   & Multi-sample consistency & No & 0.568 & 0.534 \\
EigenScore/INSIDE~\cite{chen2024inside}         & Hidden-state spectral    & No & 0.564 & 0.600 \\
RefChecker~\cite{hu2024refchecker}              & Reference-based          & No & 0.602 & 0.572 \\
P(True)~\cite{kadavath2022language}             & Output confidence        & No & 0.753 & 0.541 \\
Focus~\cite{focus2024}                          & Uncertainty scoring      & No & 0.780 & 0.526 \\
Perplexity/entropy~\cite{varshney2023stitch}    & Output probability       & No & 0.620 & 0.713 \\
LN-Entropy~\cite{varshney2023stitch}            & Output uncertainty       & No & 0.761 & 0.707 \\
ReDeEP~\cite{redeep2024}                        & Attention + FFN          & Partial & 0.762 & 0.750 \\
LUMINA~\cite{lumina2024}                         & Context--knowledge       & No & 0.769 & 0.745 \\
SAPLMA~\cite{azaria2023saplma}                  & Hidden-state probe       & No & 0.807 & 0.760 \\
\midrule
\textbf{Ours: Fused CEV/IAV}                    & \textbf{Residual + FFN}  & \textbf{Yes} & \textbf{0.8596} & \textbf{0.8689} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!htb]
    \centering
    \includegraphics[width=0.90\textwidth]{Images/compare_prior_work_ragtruth_paper_style.png}
    \caption[Baselines vs.\ proposed probe.]{Published baselines vs.\ our fused CEV/IAV probe on RAGTruth. Our method tops the chart on both backbones—and is the only one that feeds its score into a closed-loop controller.}
    \label{fig:baselines}
\end{figure}

%% -------------------------------------------------
\section{Vanilla RAG vs.\ Closed-Loop RAG}
\label{sec:vanilla_vs_cl}

This is arguably the most important experiment in the thesis: does the closed loop actually \emph{help}? Table~\ref{tab:vanilla_vs_cl} compares the delivered hallucination proxy between vanilla (single-pass) RAG and our closed-loop system.

\begin{table}[!htb]
\centering
\caption{Vanilla RAG vs.\ closed-loop RAG: delivered hallucination proxy comparison.}
\label{tab:vanilla_vs_cl}
\begin{tabular}{@{}lcccrr@{}}
\toprule
\textbf{Backbone} & \textbf{Vanilla Proxy} & \textbf{CL Proxy} & \textbf{Reduction} & \textbf{Accept} & \textbf{Abstain} \\
\midrule
Mistral-7B  & 0.3301 & \textbf{0.1068} & 67.6\% & 62\% & 38\% \\
Qwen3-8B    & 0.5388 & 0.1116          & \textbf{79.3\%} & 42\% & 58\% \\
LLaMA-3-8B  & 0.3321 & 0.1824          & 45.1\% & \textbf{90\%} & 10\% \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!htb]
    \centering
    \includegraphics[width=0.90\textwidth]{Images/vanilla_vs_closedloop_comparison.png}
    \caption[Vanilla vs.\ closed-loop RAG.]{The closed-loop system cuts delivered hallucination risk substantially on all three backbones, with different safety--utility trade-offs per model.}
    \label{fig:vanilla_vs_cl}
\end{figure}

The numbers speak clearly. Every backbone sees a meaningful reduction in delivered hallucination risk—ranging from 45\% (LLaMA) to nearly 80\% (Qwen). But this comes at different costs. Qwen achieves the biggest reduction by being the most aggressive abstainer (58\% of queries refused). Mistral strikes a balance (62\% accepted, 68\% reduction). LLaMA keeps almost everything (90\% accepted) but pays for it with a smaller reduction.

\textbf{An important caveat.} These reductions are measured using the system's own probe score as the hallucination proxy—not human judgments. The metric is therefore partly self-referential: a controller that only accepts low-scoring responses will, by construction, report low delivered risk. Three specific limitations follow: (i) the probe cannot catch the failure mode from Section~\ref{sec:illustrative_examples}; (ii) 100 queries at one seed carries sampling noise; (iii) the RE-RETRIEVE branch was rarely triggered. We expand on these in Section~\ref{sec:limitations}.

%% -------------------------------------------------
\section{Ablation Study: CEV, IAV, and Fusion}
\label{sec:ablation}

Is fusion actually worth the trouble? Could we just use one probe? Table~\ref{tab:ablation} answers this directly.

\begin{table}[!htb]
\centering
\caption{Probe ablation: accuracy with CEV-only, IAV-only, and mean-fusion.}
\label{tab:ablation}
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Backbone} & \textbf{CEV-only} & \textbf{IAV-only} & \textbf{Mean-fusion} & \textbf{Winner} \\
\midrule
Mistral-7B  & 0.772 & 0.778 & \textbf{0.781} & Fusion \\
Qwen3-8B    & 0.799 & 0.793 & \textbf{0.800} & Fusion \\
LLaMA-3-8B  & \textbf{0.789} & 0.782 & 0.782 & CEV \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!htb]
    \centering
    \includegraphics[width=0.85\textwidth]{Images/section12_3_core_ablation_accuracy_comparison.png}
    \caption[Ablation results.]{CEV-only vs.\ IAV-only vs.\ mean-fusion accuracy. Fusion wins on two out of three backbones; on LLaMA, CEV alone matches fusion.}
    \label{fig:ablation}
\end{figure}

For Mistral and Qwen, fusion wins—even if the margins are small. For LLaMA, CEV alone matches fusion while IAV lags slightly. The overall conclusion is that fusion is the safer default: it never hurts meaningfully, and on most backbones it helps.

%% -------------------------------------------------
\section{HaluEval Out-of-Distribution Analysis}
\label{sec:halueval_ood}

Table~\ref{tab:halueval_ood} shows what happens when our RAGTruth-trained probes encounter data they were never trained on. The results are backbone-dependent in a dramatic way.

\begin{table}[!htb]
\centering
\caption{HaluEval out-of-distribution AUROC.}
\label{tab:halueval_ood}
\small
\begin{tabular}{@{}l l l l l@{}}
\toprule
\textbf{Backbone} & \textbf{CEV} & \textbf{IAV} & \textbf{Mean/Fused} & \textbf{What Happens} \\
\midrule
Mistral-7B & 0.106 / 0.894 adj. & 0.175 / 0.825 adj. & 0.105 / 0.895 adj. & Polarity flips \\
Qwen3-8B   & 0.604 & 0.723 & 0.699 & Aligned transfer \\
LLaMA-3-8B & 0.740 & 0.816 & \textbf{0.814} & Best aligned \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!htb]
    \centering
    \includegraphics[width=0.90\textwidth]{Images/halueval_three_column.png}
    \caption[HaluEval OOD results.]{HaluEval transfer results. LLaMA and Qwen generalize well; Mistral's signal is equally strong but polarity-inverted.}
    \label{fig:halueval}
\end{figure}

LLaMA transfers beautifully (0.814 raw AUROC). Qwen does respectably (0.699). Mistral is the odd one out—its probe separates the classes \emph{extremely} well (adjusted AUROC of 0.895), but in the wrong direction. The hallucination signal is there; it just points the opposite way on this benchmark. This is the polarity inversion we discussed in Chapter~\ref{ch:probe_training}.

%% -------------------------------------------------
\section{Nine-Query Live Demonstration}
\label{sec:nine_query_demo}

To complement the aggregate statistics, we ran all three backbones on a curated set of nine queries—ranging from easy factual questions to deliberately impossible ones. Table~\ref{tab:nine_query} shows exactly what each backbone did.

\begin{table}[!htb]
\centering
\caption{Nine-query outcomes (fused hallucination score in parentheses).}
\label{tab:nine_query}
\footnotesize
\begin{tabularx}{\textwidth}{@{}c p{3.5cm} X X X@{}}
\toprule
\textbf{\#} & \textbf{Query Type} & \textbf{Mistral-7B} & \textbf{Qwen3-8B} & \textbf{LLaMA-3-8B} \\
\midrule
1 & Author (answerable) & ACCEPT (0.59) & ABSTAIN (0.72) & ACCEPT (0.07) \\
2 & Westphalia (answerable) & ACCEPT (0.60) & ACCEPT (0.42) & ACCEPT (0.08) \\
3 & Kelvin (answerable) & ACCEPT (0.42) & ACCEPT (0.33) & ACCEPT (0.19) \\
4 & Breakfast (impossible) & ACCEPT (0.12) & ABSTAIN (0.86) & ACCEPT (0.19) \\
5 & Mars capital (impossible) & ACCEPT (0.17) & REGEN$\times$2$\to$ACCEPT (0.20) & ACCEPT (0.17) \\
6 & Climate (RAG-supported) & ACCEPT (0.34) & ACCEPT (0.38) & ACCEPT (0.47) \\
7 & Shakespeare (subjective) & ACCEPT (0.08) & ACCEPT (0.22) & ACCEPT (0.10) \\
8 & Five planets (answerable) & ACCEPT (0.52) & REGEN$\times$2$\to$ACCEPT (0.51) & ACCEPT (0.11) \\
9 & Atlantis census (nonsensical) & ACCEPT (0.31) & ABSTAIN (0.80) & ACCEPT (0.10) \\
\midrule
\multicolumn{2}{@{}l}{\textbf{Action totals (accept/abstain)}} & \textbf{9 / 0} & \textbf{6 / 3} & \textbf{9 / 0} \\
\bottomrule
\end{tabularx}
\end{table}

The pattern reinforces what the aggregate numbers already told us. Qwen3-8B is the cautious one—it abstains on queries 1, 4, and 9, all of which involve uncertainty or impossibility. Mistral and LLaMA accept everything, but crucially, their accepted responses for impossible queries are \emph{safe refusals} rather than fabrications (e.g., ``cannot answer from the context'' for query 4). The system's safety comes from different mechanisms depending on the backbone: Qwen uses explicit abstention, while Mistral and LLaMA use in-text hedging that the probe scores as low-risk.

%% -------------------------------------------------
\section{Cross-Model Summary}
\label{sec:cross_model_summary}

Table~\ref{tab:cross_model} consolidates everything into one view—the ``at a glance'' table for the entire experimental evaluation.

\begin{table}[!htb]
\centering
\caption{Cross-model comparison summary.}
\label{tab:cross_model}
\begin{tabular}{@{}lccc@{}}
\toprule
\textbf{Metric} & \textbf{Mistral-7B} & \textbf{Qwen3-8B} & \textbf{LLaMA-3-8B} \\
\midrule
Fused validation AUROC    & 0.8596 & \textbf{0.8808} & 0.8689 \\
CEV AUROC                 & 0.8474 & \textbf{0.8763} & 0.8579 \\
IAV AUROC                 & 0.8474 & \textbf{0.8709} & 0.8622 \\
Mean-fusion accuracy      & 0.7806 & \textbf{0.7995} & 0.7822 \\
HaluEval mean AUROC (raw) & 0.105 inv. & 0.699 & \textbf{0.814} \\
Vanilla delivered proxy   & 0.3301 & 0.5388 & 0.3321 \\
Closed-loop proxy         & \textbf{0.1068} & 0.1116 & 0.1824 \\
Relative reduction        & 67.6\% & \textbf{79.3\%} & 45.1\% \\
Acceptance rate           & 62\% & 42\% & \textbf{90\%} \\
Nine-query (accept/abstain) & 9/0 & 6/3 & 9/0 \\
Fusion weight (CEV/IAV)   & 0.50/0.50 & 0.60/0.40 & 0.45/0.55 \\
\bottomrule
\end{tabular}
\end{table}

No single backbone dominates every metric. Qwen3-8B wins on in-domain detection quality. LLaMA-3-8B-Instruct wins on out-of-domain generalization and utility preservation. Mistral-7B-Instruct-v0.2 offers the most balanced safety--utility profile. The right choice depends entirely on what you prioritize—and that, ultimately, is a deployment decision rather than a research one.
